// A alocação estática define o tamanho da memória em tempo de compilação,
// enquanto a alocação dinâmica ocorre em tempo de execução, permitindo
// flexibilidade no uso da memória.

// A alocação dinâmica usa funções como malloc (para alocar), realloc
//(para redimensionar) e free (para liberar), sendo essencial para
// manipulação de estruturas de dados variáveis.