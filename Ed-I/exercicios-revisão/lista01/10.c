// a) Válida, o ponteiro recebe a primeira posição do 
// vetor mat somado mais 1 ou seja, mat[1].

// b) inválida, não é possivel pois array é um ponteiro 
// e ele n pode ser modificado

// c)Inválida, Assim como na expressão anterior, 
// mat é um ponteiro constante e não pode ser alterado. 

// d) Válida, atribui o valor de mat[0] a x e incrementa mat[0] em 1 