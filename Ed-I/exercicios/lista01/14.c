

// vet[0] //refere-se ao primeiro elemento do vetor.

// int *ptr;   //Declara um ponteiro para um inteiro

// int vet[] = {1, 2, 3, 4, 5};
// int *ptr = vet;  // Ponteiro aponta para o início do vetor

// ptr + 1 //desloca o ponteiro para o próximo elemento do vetor.
// *(ptr + 1) //desreferencia o ponteiro para acessar o valor do segundo elemento.

// int x = 10;
// int *ptr = &x;  // Ponteiro para a variável x
// printf("%d\n", *ptr);  // Exibe o valor de x (10)

// *ptr //acessa o valor de `x` através do ponteiro.

// int x = 20;
// int *ptr = &x;  // Ponteiro recebe o endereço da variável x
// printf("%p\n", ptr);  // Exibe o endereço de x

// &x //retorna o endereço de `x`.

// for (int i = 0; i < 5; i++) {
//     printf("Elemento %d: %d\n", i, *(ptr + i));  // Acessa o i-ésimo elemento via ponteiro
// }
